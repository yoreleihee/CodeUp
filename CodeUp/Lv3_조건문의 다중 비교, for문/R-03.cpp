#include <iostream>

using namespace std;

int main()
{
    int a, b, c, d;

    cin >> a >> b >> c >> d;

    if (a == 1 && b == 2 && c == 3 && d == 4){
        cout << "로그인성공";
    }
    return 0;
}